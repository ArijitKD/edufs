# edufs
An educational in-memory filesystem built from scratch in C, with recursive directory support, using DSA concepts and industry-grade design.

## Currently supports
- File + directory creation
- File + directory deletion (both subtrees and leaf nodes)

## TODO
- Moving
- Copying
- Frontend + Command support (`mv`, `cp`, `rm`, `mkdir`, `touch`, `ls`)

## AI Disclaimer
This project has no AI-generated code. Idea and implementation are completely the author's. AI was used only for tracking down subtle bugs and
occasional referencing.
